version: '3.8'
services:
  app:
    image: node:latest
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:CdekxDBWZzJbjtUYlCVOqtoBDtKVowTd@postgres.railway.internal:5432/railway
  bd:
    image: postgres:15-alpine
    environment:
      POSTGRES_PASSWORD: example
    ports:
      - "5432:5432"